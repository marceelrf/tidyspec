% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_pca_individuals.R
\name{spec_pca_individuals}
\alias{spec_pca_individuals}
\title{Create a PCA Individuals (Scores) Plot}
\usage{
spec_pca_individuals(
  .data,
  pc_x = 1,
  pc_y = 2,
  color_var = NULL,
  label_points = FALSE,
  point_size = 3,
  alpha = 0.7
)
}
\arguments{
\item{.data}{Result object from `prcomp()` function}

\item{pc_x}{Principal component to plot on x-axis (default: 1)}

\item{pc_y}{Principal component to plot on y-axis (default: 2)}

\item{color_var}{Optional variable to color points by (length must match input data)}

\item{label_points}{Logical, whether to label points (default: FALSE)}

\item{point_size}{Size of points (default: 3)}

\item{alpha}{Point transparency (default: 0.7)}
}
\value{
A ggplot object showing the PCA scores plot
}
\description{
Generates a 2D plot of individual observations based on their PCA scores.
}
\details{
The plot displays:
- Each observation as a point in PC space
- Option to color by a grouping variable
- Percentage of explained variance for each axis
}
\examples{
\dontrun{
# Basic usage:
pca_result <- prcomp(iris[,1:4], scale. = TRUE)
spec_pca_individuals(pca_result)

# With coloring by species:
spec_pca_individuals(pca_result, color_var = iris$Species)

# Customizing components:
spec_pca_individuals(pca_result, pc_x = 1, pc_y = 3)
}

}
